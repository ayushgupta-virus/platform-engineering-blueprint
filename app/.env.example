# Local development configuration for the sample app.
# In Azure these values come from a ConfigMap + Key Vault (never commit real secrets).
PORT=3000
APP_ENV=local
LOG_LEVEL=info

PGHOST=localhost
PGPORT=5432
PGUSER=appuser
PGPASSWORD=appsecret
PGDATABASE=appdb
PGSSLMODE=disable
