apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-api-config
  namespace: app
data:
  APP_ENV: "base"
  LOG_LEVEL: "info"
  PORT: "3000"
  PGPORT: "5432"
  PGDATABASE: "appdb"
  PGSSLMODE: "require"
  # PGHOST / PGUSER are patched per environment via overlays.
